<!DOCTYPE html>
<html>
  <head>
    <title>AlexisPresident - Test</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="style.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <style>
      /* Chatbot Widget CSS */
      #chatbot-widget {
        position: fixed;
        bottom: 20px;
        right: 20px;
        width: 300px;
        border: 1px solid #ccc;
        border-radius: 10px;
        background-color: #fff;
      }

      #chatbot-header {
        background-color: #007bff;
        color: #fff;
        padding: 10px;
        font-weight: bold;
        border-top-left-radius: 9px;
        border-top-right-radius: 9px;
      }

      #chatbot-messages {
        max-height: 300px;
        overflow-y: auto;
        padding: 10px;
      }

      #chatbot-form {
        display: flex;
        padding: 10px;
      }

      #chatbot-input {
        flex-grow: 1;
        margin-right: 10px;
        padding: 5px;
        border-radius: 5px;
        border: 1px solid #ccc;
      }

      #chatbot-input:focus {
        outline: none;
        border-color: #007bff;
      }

      #chatbot-form button {
        padding: 5px 10px;
        border-radius: 5px;
        border: none;
        background-color: #007bff;
        color: #fff;
        cursor: pointer;
      }

      #chatbot-form button:hover {
        background-color: #0069d9;
      }

      .chatbot-message {
        margin-bottom: 10px;
      }

      .user-message {
        text-align: right;
      }

      .user-message .message-text {
        background-color: #007bff;
        color: #fff;
        border-radius: 10px;
        padding: 10px;
        display: inline-block;
        max-width: 80%;
        word-wrap: break-word;
      }

      .chatbot-message .message-text {
        background-color: #f4f4f4;
        color: #333;
        border-radius: 10px;
        padding: 10px;
        display: inline-block;
        max-width: 80%;
        word-wrap: break-word;
      }

      .chatbot-message .message-text a {
        color: #007bff;
      }
    </style>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-12">
          <h1>Welcome to AlexisPresident - Test</h1>
          <p>This is a test website for AlexisPresident.</p>
        </div>
      </div>
    </div>
    
    <!-- Chatbot Widget -->
    <div id="chatbot-widget">
      <div id="chatbot-header">
        Chatbot
